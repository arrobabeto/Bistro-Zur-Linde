---
import Base from "~/layouts/Base.astro"
import { DEFAULT_LOCALE } from "~/config/locales"
import { phrases } from "~/lib/phrases"
import { translate } from "~/lib/i18n"
import { siteName } from "~/lib/site"

const locale = DEFAULT_LOCALE
const title = translate(phrases.page_not_found, locale)
---

<Base locale={locale}>
  <Fragment slot="head">
    <title>{title} · {siteName()}</title>
    <meta name="robots" content="noindex" />
  </Fragment>
  <main class="container-prose py-section text-center">
    <p
      class="text-brand-900 mb-3 text-xs font-semibold tracking-[0.18em] uppercase"
    >
      404
    </p>
    <h1 class="mb-4 text-4xl font-semibold tracking-tight">{title}</h1>
    <p class="mb-8 text-neutral-600">
      {translate(phrases.page_not_found_description, locale)}
    </p>
    <a href="/" class="btn btn-secondary">
      {translate(phrases.homepage, locale)}
    </a>
  </main>
</Base>
